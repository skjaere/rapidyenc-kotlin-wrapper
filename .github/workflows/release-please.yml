name: Release Please

on:
  push:
    branches:
      - master

jobs:
  release:
    uses: skjaere/debridav-ci/.github/workflows/reusable-release-please.yml@main
    permissions:
      contents: write
      pull-requests: write
